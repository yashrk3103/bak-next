// TODO: Implement PackagesSection — share screenshot/design when ready
// Expected: Grid of package cards (goat, cow shares, etc.) with prices

export default function PackagesSection() {
  return (
    <section id="packages" className="placeholder-packages">
      {/* Design to be implemented */}
    </section>
  );
}
